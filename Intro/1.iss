; This is a comment in a LavishScript script!

; This is a function! The function called "main" gets called when the script starts.
function main()
{
    ; All this function does is print out "Hello World!" in the console
    echo "Hello World!"

    ; The function ends when it hits the }. When the main function ends, the script stops.
}